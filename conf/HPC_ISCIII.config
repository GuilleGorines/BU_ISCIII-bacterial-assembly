/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run nf-core/assemblybacterias -profile test,<docker/singularity>
 */

params {
  config_profile_name = 'Instituto de Salud Carlos III, High Performance Computer (HPC-ISCIII)'
  config_profile_description = 'Profile containing the paths used in the HPC in the ISCIII, intended for direct usage inside this institution'

  // Limit resources
  // max_cpus =
  // max_memory = 
  // max_time = 

  kmerfinder_bacteria_database = "/data/bi/references/kmerfinder/20190108_stable_dirs/bacteria"

  // Input data
  // TODO nf-core: Specify the paths to your test data on nf-core/test-datasets
  // TODO nf-core: Give any required params for the test so that command line flags are not needed
  schema_ignore_params = 'genomes,input'
}

process {

  executor = 'sge'
  queue = 'all.q@obelix10,all.q@obelix11,all.q@obelix12,all.q@obelix13,all.q@obelix08,all.q@obelix09'
  conda = '/data/bi/pipelines/miniconda3/envs/assembly'
  penv = 'openmp'
  
  withName:KMERFINDER {
    singularity {
      enabled = true
      autoMounts = true
    }
    container = '/data/bi/pipelines/kmerfinder_v3.0.simg'
  }
}   
